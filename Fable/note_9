The log settles it. With real numbers across the whole probe set, the terminal-silence gate is now falsified as a hypothesis: it produced **zero true saves and three false keeps**. Look at what `kept:long-tail` actually protected — whisper g2 (0.66×, ear-confirmed moan), peter g4 (0.66×, ear-confirmed moan, blocked by 20 ms), and the warmup's g1 (0.63×, tail 1.78 s — almost certainly a moan too, nobody's listened to warmup). Meanwhile the case the gate was built to protect — a real soft word followed by dead pad tail — turns out not to exist in the data: the whisper probe was *engineered* to produce it, and Kokoro instead rendered "stop" attached inside g1. The earlier "stop at 0.841×/3.28–3.64 s" interpretation was wrong; the gate was calibrated on a misidentified moan.

So rather than Grok's option A (raise ceiling to 0.95 *and* add a long-tail min-ratio — two new constants propping up a gate whose every observed action was wrong), I'm taking the simpler falsification-driven path Grok listed as the alternative: **remove the terminal gate entirely.** The empirical separation across every ear-confirmed case is now clean with four tests:

- Every true moan: weak (0.23–0.77×) AND short (0.12–0.40 s) AND detached (gap 0.40–0.78 s)
- Every true keep: loud (≥1.05×) OR long (>0.6 s) OR attached (≤0.12 s) — every real word has failed at least one strip test, always

The detachment gate keeps its two ear-confirmed true saves (wallet "port", fox's 0.02 s blip). `tail=` stays in the debug log as pure data collection, in case a genuine weak-short-detached real word ever shows up — if it does, the right discriminator is probably spectral (moans are breathy/noise-like, words have formant structure), and we'll have the logged evidence to design it. Constants-tweaking can't separate that case, so I'd rather hold the simple ruleset until data demands otherwise.**v1.1.5** is ready. One change: the terminal-silence gate is deleted (knob, branch, and docstring), with the falsification rationale recorded in the docstring so the reasoning survives in the repo. The ruleset is back to four tests — weak + short + detached + in-window — plus the healthy ref and floor from v1.1.4. `tail=` remains in the debug output purely as evidence collection.

The suite this round is built from the **actual v114 log numbers**, not my synthetic guesses: peter's moan (0.66×, gap 0.62, tail 0.82) now strips → cut ~3.19 s, matching the v113 clean reference; whisper's moan (0.66×, gap 0.64, tail 1.16) strips → cut ~2.67 s, just past end of g1; the warmup's 0.63× group strips too. Regression replicas from the same log all hold: wallet port `kept:attached`, fox's blip `kept:attached` with g4 stripped, full_repro, s1/s4 shapes, the 1.39× continuation, and the ref-floor case.

Prediction for Grok's re-probe, stated up front so the test can falsify me: expected durations ≈ **peter 3.19 s, whisper ~2.67 s, wallet 4.39 s (unchanged), full_repro 16.09 s (unchanged), fox 3.21 s (unchanged)**, with ear PASS on all seven. The one clip worth extra attention is **whisper_stop** — my expected cut assumes Grok's revised interpretation is right that "stop" lives inside g1 (ends ~2.64 s). If the ear instead finds "stop" missing from the 2.67 s WAV, then g2 was the word after all, both interpretations have now failed once, and the next step is spectral discrimination rather than another threshold — the v113 4.78 s artifact contains both candidate regions for offline analysis, so we wouldn't even need a new render to settle it.

If all seven pass, the trim thread closes and what remains on the board is unchanged: the WebUI Read Aloud multi-POST behavior under RTF ≫ 1 (client leg, untouched by any of this), then the queued items from the status doc — repo commit of the final server, the two OpenVINO issue filings, and the stress-test spec.