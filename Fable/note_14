The `False` results need explanation before I hand this over — let me verify precisely what differs from the validated build:My earlier identity check was sloppily scoped (it swept the whole span between functions, catching intentional additions), so I re-verified properly: **the trim function is byte-identical to validated v1.1.5**, and the full diff against that build contains exactly the intended changes and nothing else — the Response Splitting docs (v1.1.5-doc), `WARM_BUCKETS` parsing + env docs, the `openvino=` startup log line, the `_prewarm_text_for` helper, the synthesize-based pre-warm block, and the version string. That's **v1.1.7**, published above. It codifies what Grok validated by hand — pre-warm via real synthesis — with the text grown to *near bucket capacity* (91/187/283/379/511 real tokens for the five buckets in mock-tokenizer tests), because tonight's data says capacity is what matters: the 15-real-token `"Warm up."` synthesis never warmed fox, the ~96-token one did.

And that fact pattern is worth pausing on, because it quietly reshapes the mystery. Three measured facts from tonight: a 15-real-token synthesis doesn't warm a 55-token request; an all-zeros bucket infer (31 s!) doesn't either; a ~96-real-token synthesis does. So whatever the lazy setup keys on, it's not "the compiled bucket" — it tracks something about the *internal, data-dependent* work (the duration predictor's frame count scales with real tokens; pads and short texts produce different internal shapes). Which sharpens what the v114 anomaly needs to explain: five sequential real-text bucket-96 requests at 44–73 real tokens each, *none* warming the next. Under tonight's behavior, s1 (73 tokens, the largest) should have warmed everything after it. It didn't. So the anomaly stands as Grok's amended note records it — but there's now one cheap, decisive experiment that bounds the product claim either way:

**After a v1.1.7 pre-warm on bucket 96, send first-ever requests of s1, s2, s3, s4 in sequence — novel content, varied lengths, same bucket.** All four at ~2.9 s / RTF ~0.9 means warm state transfers across content and lengths below the pre-warm size, and "ov-gpu is ~0.9 RTF for varied text once warmed" is a solid README claim. Any of them at ~18 s means cold is per-content/per-length, the claim shrinks to "repeated content only" — and it would also retroactively explain v114, converting the anomaly into a understood behavior with a measured boundary. Either outcome is worth a comment on OpenVINO issue 2 after filing: "first-infer setup" with data on what does and doesn't count as first is exactly the kind of characterization upstream can act on.

One small ops note for the README alongside `KOKORO_WARM_BUCKETS=96,192`: each warmed bucket costs its cold time at startup (~19 s for 96, more for larger buckets — the 288 pre-warm will be ~50 s based on the n=250 cold measurement), so warming all five buckets is a ~2-minute startup. Worth stating so operators choose buckets matching their actual traffic — for Open WebUI with Splitting=None, that's mostly 288/384.