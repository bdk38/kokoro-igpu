v1.1.1 read and understood — including confirming there's still **no lock** anywhere in it, so the `Infer Request is busy` 500s are expected under concurrent WebUI posts. Grok's diagnosis in the note looks right to me, and I can back skip (1) with numbers:

"Wait, did you remember the keys, the wallet, and the passport?" is 63 tokens → +2 boundary tokens = 65 → bucket 96, so `pad_frac ≈ 0.32`, and the search window is `0.32 × 1.5 ≈ 48%` of the chunk. The trim is allowed to cut anywhere in the **back half of the sentence**, and its rule is "first 100 ms of sustained quiet wins." A comma/list pause is routinely 100–300 ms of near-silence. So it cuts at "the wallet, ▁" and drops the rest — exactly what you heard. The v1.1.1 rule fixed the moan by introducing a false-positive on natural pauses. Note this also predicts the single-curl full-text test isn't automatically clean: 249 tokens → bucket 288 gives a window over the last ~20%, which still contains mid-sentence pauses of the final sentence.

The correct discriminator was in Grok's own next-action #2: don't cut on quiet alone — cut only when what *follows* the quiet actually looks like a pad burst. I've implemented that, plus the OV lock and trim logging, as **v1.1.2**:

**Trim rewrite (structure-aware, cuts only at group boundaries):** frame the whole chunk into RMS groups, merging speech runs separated by gaps shorter than `TRIM_QUIET_S`. Then walk backwards from the end and strip a trailing group only if it qualifies as a pad burst on **all three** tests: preceded by a ≥100 ms quiet gap, peak RMS below `TRIM_MOAN_RMS_RATIO` (0.6) × the head reference, and shorter than `TRIM_MOAN_MAX_S` (0.8 s). "and the passport?" fails the ratio *and* the duration test, so it survives; the measured moan (quieter, brief, gap-separated, then silence) is stripped. If no group qualifies, it just trims trailing silence after the last group — never mid-speech, fail-safe direction is "keep audio."

**OV infer lock:** `threading.Lock` around compile+infer in `OvBackend`, so concurrent WebUI posts queue instead of 500ing. It also closes a latent double-compile race in `_get`. ORT's `run()` is thread-safe, left unlocked.

**Trim diagnostics (Grok's action #4):** set `KOKORO_TRIM_DEBUG=1` and every trim prints `[trim] n_real/n_bucket, groups, stripped, cut point` — so the next listen test comes with per-chunk evidence attached.

I verified the logic against synthetic cases before handing it over:That's **v1.1.2**, built on Grok's v1.1.1, with three changes and test evidence for each:

**Trim rewrite.** The chunk is segmented into speech groups (gaps under `TRIM_QUIET_S` don't split a group), and trailing groups are stripped only if they pass all pad-burst tests: gap-separated, peak RMS < 0.6× head reference, shorter than 0.8 s, and starting inside the pad window. Synthetic tests confirm: the measured moan shape gets stripped, the "keys, the wallet, ▁ and the passport?" shape survives intact (the exact v1.1.1 regression), a pause-then-moan combo does both correctly, loud/long tails are kept, unpadded chunks are untouched byte-for-byte, and stacked double bursts get stripped.

**OV infer lock.** One `threading.Lock` around compile+infer; 8 threads × 10 infers against a fake request that raises "Infer Request is busy" on reentry ran clean. This should eliminate those 500s from the WebUI multi-POST pattern.

**Trim debug logging.** `KOKORO_TRIM_DEBUG=1` prints per-chunk `n_real/n_bucket`, group count, groups stripped, and cut point — run the next listen test with it on and the log becomes the evidence trail.

For the test protocol, I'd amend Grok's action #1 slightly: the single-curl full-text control is still worth running, but note it isn't a pure "server vs WebUI" discriminator under v1.1.1 — 249 tokens → bucket 288 gives a search window over the last ~20%, so the old trim could plausibly clip inside "Seven silver swans swam smoothly south across the sea." even in one request. Under v1.1.2 that ambiguity goes away. So the cleanest sequence: swap in v1.1.2, restart with `KOKORO_TRIM_DEBUG=1`, curl the full repro text as one request and listen (expect: complete, no moan, debug lines showing `stripped=1` only where a real burst existed), then re-test WebUI Read Aloud. If skips persist in WebUI but the single-curl WAV is complete, the remaining fault is client-side — most likely playback under RTF ≫ 1 (segments arriving 5–9× slower than realtime), which no server change fixes and would argue for pointing WebUI at ort-cpu for daily use while ov-gpu stays the demo/offload path.

One thing to flag for Grok's skip (2) analysis: with the lock in place, concurrent WebUI posts now queue, so under ov-gpu the *last* segments of a long paragraph will arrive even later than before. If WebUI has a timeout per segment, serialization could convert "500 busy" into "silently missing segment." The debug log plus WebUI's network tab will show which it is.

One knob left deliberately conservative: `TRIM_MOAN_RMS_RATIO = 0.6`. If a listen test finds a louder-than-expected moan surviving, the debug line prints the ref and the group energies needed to tune it from data rather than by ear alone.