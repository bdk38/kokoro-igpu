Grok's A/B/C is a clean kill of both of my hypotheses — cache blob falsified by B (empty cache, same steady), clocks falsified by the pegged gputop during A — and the padding explanation for server 0.90 vs direct 0.62 is exactly right: bucket-96 compute billed against ~3.2 s of trimmed audio. Warm server fox at **0.90 RTF** upgrades the product story materially: the iGPU path is realtime-capable in production once shapes are warm.

I've shipped **v1.1.6** to make that operational: `KOKORO_WARM_BUCKETS=96,192` pre-warms the listed buckets at startup (full-pad dummy infer per bucket, eating the multi-second lazy setup where nobody's waiting), and the OV backend now logs `openvino=<version>` at startup for forensics. The env var is documented in the header with the measured cold/warm numbers and the "cache shortens compile, not first-infer" distinction from note 18. I byte-verified the trim function is identical to the validated v1.1.5 — this touches only startup and logging.

One piece of honest bookkeeping before note 18's conclusion #1 hardens into repo history, though: **"historical 18–25 s was cold first-infer" doesn't fully fit the v114 log.** That log shows a single process lifetime — startup warmup on bucket 96 (13 tokens → bucket 96), then *five sequential bucket-96 requests* (s1–s4, fox) at 18.5–24.8 s each. If cold-start were the whole story, s1 onward should have been ~3 s, the way request 2 was today. Something else was true that morning and isn't true tonight, and it's no longer reproducible. Candidates worth a line in the note: a mid-day venv or driver change (nothing logged the OV version then — hence the new startup line), a competing GPU consumer during the probe sessions, or a power/scheduling state that gputop wasn't watching at the time. I'd suggest amending note 18's interpretation from "was cold first-infer" to "is now cold-first-infer-only; the v114-era all-requests-slow behavior is unexplained and unreproducible — anomaly on record, forensic logging added." The distinction matters because if it recurs in the field, the record should say "we saw this once" rather than "this can't happen."

Ops guidance stays as Grok wrote it, with one addition for the README: ov-gpu is now "warm steady ~0.9 RTF at bucket 96 (~0.6 at tight shape), cold first infer per bucket multi-second — set `KOKORO_WARM_BUCKETS` for serving." And this genuinely reorders the queue: the shared-RAPL stress test is now testing a *realtime-capable* path rather than a demo, which makes it the most interesting remaining experiment in the project.