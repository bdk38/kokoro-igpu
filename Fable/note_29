v1.1.8 closed the way it should — and the smoke table is quietly the best validation the shape-key work has gotten. Three numbers in one log tell the whole story we spent notes 18–20 untangling: fox after pin at 0.95 RTF (warm-class, the pinned shape), novel text at 7.35 (cold shape, honesty holds), fox repeat at 0.97 (warm state persists). That's `KOKORO_WARM_TEXT` doing exactly what it was designed to do, demonstrated on the wire through the real request path rather than a harness — which was the whole lesson of the zeros-vs-real-text fix. The docstring claims and the observed behavior now match, which is the only definition of shipped that counts.

One small cross-note observation, informative only: the server's warm pinned RTF (~0.95) sits above the direct-harness warm steady from the rollback smoke (~0.66). No alarm — different measurement paths, and the server carries bucket padding in its denominator per measurement-honesty item 4 — but if anyone ever quotes the two numbers side by side, that's the explanation, and it's worth a passing line whenever the commit-set notes get finalized so nobody rediscovers it as a discrepancy.

Ship queue state as I read it now: v1.1.8 done, which promotes the **repo commit** to the head — and it's grown, since the commit set now includes the spike tree, notes/25–38, both Fable notes 26–27, WORKFLOW, and the v1.1.8 working tree itself (note 35 says working tree → commit, so the push formalizes it). Filings stay parked behind S0's A1/A2 data by design. After the commit, the only open items are your two boxes in 36b — S0 ack with priority, and ear ownership — and then Grok has a clear runway into S0.1. The response/chunk cache design greenlight is the one thing you could hand me in parallel at any point, since it's backend-agnostic and untouched by every branch of the S0 ladder.