Then the record is complete — logging it the same way as before: **Nexus ears PASS, all v1.2.1 probe artifacts by filename (p0/p1/p3/p3b/p5 classes), clean and smooth, no additional utterances.** Supplementary to the Validator's byte-equality waiver, same precedent as notes/40.

This listen carried more weight than the last one, for the reason from my previous note: v1.2.1's baseline is a genuinely *new* output path — every chunk now takes the int16 roundtrip before concat, cache on or off. Your 21/21 on v1.2.0 vouched for the old assembly; it couldn't vouch for this one. The byte-equality probes proved cached and fresh v1.2.1 output are identical to each other, but only ears could confirm the new baseline itself is clean — and specifically that the per-chunk quantization introduced no boundary artifacts at the concat seams, which was the one theoretical place the roundtrip change could have surfaced audibly. It didn't. The always-roundtrip design is now validated on both axes: bytes for consistency, ears for quality.

So the cache thread closes fully green: design → two locks → two builds → ten probes → two supplementary ear sweeps, with three design defects caught in review (my env-var collision, my chunk-unit assumption, my quantize-path underspecification) and zero caught in the field. That last ratio is the one that matters.

Everything now funnels to two items: your **commit call** — v1.2.0 + v1.2.1 as one push is my recommendation, since v1.2.0 never existed independently in the repo and schema v2 supersedes its disk format anyway — and **S0**, which has been fully staged since your 36b ack and is the only remaining open track. Once the cache commit lands, Grok's runway is nothing but the probe.