Read both. The workflow doc is good — and the main reason it's good is that it's descriptive rather than aspirational. Nearly every rule in it traces to a scar: canonical `notes/` came from the Warm Bucket Fix write-up divergence, "ears beat metrics" from the trim saga, the cold-vs-steady methodology rule from the note_11 catch, the shell-quoting and pkill hygiene from wasted cycles we actually logged. That's the right way to write one of these. The peer structure (Fable ⟂ Grok under Nexus) also matches how the project actually caught its worst bugs — the methodology catches happened *because* neither AI is downstream of the other. Keep that structural rule exactly as written.

Three suggested edits before we adopt it, in descending order of importance:

**1. Spike gates must be written before implementation begins.** The doc says spike **go** only if Profiler + Validator clear "the agreed gate" — but nothing requires the gate to exist in writing before the Mechanic starts. That's the one place this org chart could drift: a spike that defines its success criteria after seeing results isn't falsifiable, and goalpost creep is exactly the failure mode our prediction discipline exists to prevent. One added sentence under the spike lens: *"The go/kill gate (parity threshold, cold/warm/restart criteria, CACHE_DIR persistence requirement) is written in a Fable note and acknowledged by Nexus before decoder-export implementation starts."* I'll write that gate as my first spike deliverable, so this costs nothing.

**2. Mechanical enforcement of the ship-path freeze.** The RACI marks "ship-path freezes: Grok orch. R (enforce)" but gives no mechanism. With Grok Build running `--always-approve`, discipline sections aren't enforcement. Concrete rule: spike work lives in its own directory (e.g. `spike/` or a branch), and `scripts/kokoro_server.py` plus the patched ONNX under `models/patched/` are off-limits to Build prompts during spike sessions — if a spike result needs a server change, it comes back through the normal ship-path loop. Cheap to state, prevents the worst possible accident.

**3. Treat the RACI tables as reference, not process.** For a three-party team the operative core is the handoff contract diagram plus the four acceptance lenses — those are what people will actually follow mid-session. Two full RACI templates is more maintenance surface than the team size justifies, and stale RACI is worse than none. I wouldn't delete them; I'd add one line saying the handoff contract governs when they conflict, so nobody has to keep them synchronized under pressure.

One nit, non-blocking: the "prefer non/low-reasoning worker models" rule for sub-agents is right for retrieval and log-grep fan-out, but the repro technician drafting one-command upstream repros probably wants a reasoning model — those repros are the credibility of our issue filings. I'd leave that to Grok's per-dispatch judgment rather than encode it.

That's the whole review. Nothing structural needs to change — the doc freezes a loop that demonstrably works.

On starting the spike: per note_16 the sequence is strict-load fix → hook-validated seam B → decoder-only ONNX at fixed T → cold/warm/restart + CACHE_DIR matrix. If you're good with the three edits (Grok can fold them in as maintainer), my first deliverable is the next Fable note containing (a) the written go/kill gate per edit #1 — including the parity bar that the current maxdiff ~0.075 has to beat once strict-load is fixed, and (b) the forward-hooks validation ladder design for isolating the seam B mismatch, with predicted branches: if it's the `strict=False` load silently dropping weight keys, hook-level divergence should first appear at a specific named module rather than accumulating gradually. Say the word and I'll write it.