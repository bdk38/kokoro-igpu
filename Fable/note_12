Reviewed both updated drafts end to end, re-ran every division against the pasted captures, and cross-checked the new disambiguation table for internal coherence. **Green light — these are filing-ready.** The only remaining steps are mechanical: file issue 1, paste its URL into issue 2's companion line, file issue 2, cross-link back.

What got fixed, verified: issue 1's "Why it matters" now matches the corrected story, the node-id note covers the 93790/66245 capture variance, and the f32 control carries its own RTF annotation. Issue 2's rewrite is genuinely better than what it replaced — the honest title, the harness-vs-direct methodology note, the protocol column in the table, and the mel_L1 caveat that turns a confusing number into supporting evidence for the drift claim. The arithmetic is airtight now: steady 0.609/0.624/0.600 at n=32/53/250 all check out against their raw logs, cold ≈ 4.2–5.4×, ms/token *improving* with length (42.8 → 30.2), and even the harness means reverse-engineer correctly as (cold + k·steady)/(k+1). The coincidence I flagged last round — harness 9.108 s ≈ direct warmup 9.25 s — is now confirmed and documented as the root cause of the historical RTF > 1 claim. That's exactly the kind of self-correction that makes a report credible; Intel's engineers will trust numbers from people who caught their own methodology bug.

But notice what this finding actually did to the project: **steady-state ov-gpu is 0.60 RTF — sub-realtime — at ~6 W with the CPU idle.** That's the dormant-silicon thesis landing harder than we'd scored it. Which surfaces the new most-important open question: **the server measures 18–25 s per bucket-96 chunk on *every* request, including well after its startup warmup on that same bucket** — roughly 6× worse than direct steady for comparable shapes, in the same-day logs. I already eliminated the easy explanations from the server source: it reshapes static per bucket, sets `PERFORMANCE_HINT: LATENCY`, and reuses one infer request, all identical to the direct test. The one structural difference in the captures: the server loads a **CACHE_DIR blob** (compile 0.6–0.8 s) while the fresh direct runs compiled from scratch (2.2–7.4 s). So for Grok's next session, in order of information-per-minute:

1. `intel_gpu_top` **during a server request** — the attached capture was taken during a direct run; nobody has watched clocks during the server's 18 s. If `act` MHz sits near idle instead of ~1050, it's a clock/power story; if it's ~1050 at 95% busy for the full window, the work itself is bigger or slower.
2. **Move the OV cache dir aside, restart, curl the same bucket 3×.** If requests 2–3 drop to ~3.5 s, a stale cached blob deserializing into a slow executable is the culprit — and the fix is a cache wipe plus maybe versioning the cache path.
3. If both null: run the direct test *with* `--cache` pointed at the server's cache dir; if direct goes slow too, blob confirmed from the other side.

If this closes the way steady-state suggests it can, the server's ov-gpu path goes from "demo, RTF 4–6" to "~0.6–0.7 RTF production-capable," which would mean updating the README's "do not default to OV-GPU" framing and the status rollups — and it makes the queued stress-test (CPU load vs GPU clocks under shared RAPL) far more interesting, since there'd now be a realtime-capable path worth defending under load. File the issues as they stand, though — the steady/cold numbers in them are direct-test measurements and correct regardless of what the server mystery turns out to be.