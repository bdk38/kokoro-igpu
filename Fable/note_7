Grok's validation is exactly what was needed — real peak/ref numbers on both sides of the gate. The separation problem is now quantified: measured pad bursts sit at **0.64–0.825×** ref, real continuation speech at **~1.39×**. My 0.6 was calibrated on synthetic assumptions and lands inside the moan band. Retuning, with one adjustment to Grok's suggestion and one addition:

**Ratio → 0.9, not 0.85.** 0.85 leaves only 3% headroom over the loudest measured moan (0.825); 0.9 gives ~9% while still keeping 35% clearance below the measured real-speech floor (1.39).

**Duration gate tightened 0.8 → 0.6 s as the compensating safety.** Loosening the ratio that much widens the false-positive space (a soft, short final word after a pause at 0.7×ref is now strippable). All three measured moans are ≤ 0.40 s, so 0.6 s keeps 50% headroom over the worst case while halving exposure of real short words. Note the wallet continuation is doubly protected now: 1.39× peak *and* 0.9 s duration both fail the burst tests independently.

**Per-group debug logging (Grok's "optional" — promoted to required).** Every examined trailing group now logs `start–end, dur, peak/ref → verdict (stripped / kept:too-loud / kept:too-long / kept:in-head)`, so the reconciliation table Grok built by hand this round falls out of the log automatically next round.**v1.1.3** is ready. Changes against what Grok is running: `TRIM_MOAN_RMS_RATIO` 0.6 → **0.9**, `TRIM_MOAN_MAX_S` 0.8 → **0.6 s**, per-group verdict logging under `KOKORO_TRIM_DEBUG=1`, and the FastAPI version string is now regex-bumped to `1.1.3` (which also fixes the stale-string issue Grok noted).

All nine recalibrated test cases pass against Grok's measured numbers: the three surviving moans (0.64×/0.40 s, 0.74×/0.22 s, 0.825×/0.28 s) now strip; the wallet continuation (1.39×, 0.9 s) is kept by two independent gates; a soft-but-long tail (0.8×, 0.75 s) and a loud-short one (1.1×) are correctly kept; the older strip cases still strip; unpadded audio is untouched.

One honest caveat to carry into the listen pass, because it's the specific tradeoff of ratio 0.9: **a genuinely soft, short (<0.6 s) final word after a ≥100 ms pause is now inside the strip envelope** — my suite deliberately includes that case (0.70×, 0.40 s) and it gets cut. Grok's regression material (s1/s4/fox for no new last-word clip) covers this partially, but a sentence engineered to end that way would be a stronger probe — something like *"He paused, then whispered, stop."* — a quiet monosyllable after a comma. If that clips, the two candidate remedies are already separable in the log: either back the ratio off toward 0.85 (costs margin over the 0.825 moan), or add the position gate (burst must start past `audio.size × n_real/n_bucket`), which I drafted but held back because the full_repro moan starts at ~0.95× of that estimate — too close to gate on without more measured examples. The per-group debug lines will accumulate exactly those examples.

Test protocol per Grok's item 3, unchanged: swap in, restart with `KOKORO_TRIM_DEBUG=1`, re-run `scripts/probe_v112.py`, ear on `s2_wallet` / `s3_peter` / `full_repro` (moan must be gone), regression on `fox` / `s1_well` / `s4_swans` (no clip), plus optionally the whispered-ending probe above. And still open after this: the WebUI Read Aloud re-test — the server-side skip explanation is closed, so if skips persist there, it's the client playback leg under RTF ≫ 1.