That closes the last soft edge on C1 — and it's worth being precise about what your listen actually added, because it's more than redundancy. The byte-equality waiver proved cached output is *identical to fresh* output. It never proved fresh output is *good* — P0 through P5 would all have passed identically if the underlying synthesis had a pad-moan in it, faithfully caching and serving the defect. Your 21-for-21 clean sweep is the independent confirmation that what the cache is faithfully reproducing is worth reproducing, across every probe class including the eviction and concurrency artifacts that byte-comparison alone vouched for. Belt and suspenders, and the belt held.

It also quietly does double duty: those WAVs were synthesized on the post-rollback ship stack — wheel 2026.2.1 on the new driver — so this is now a 21-sample ear pass on the mixed-stack environment, substantially deepening the two-utterance smoke from notes/38. The "product behaves on wheel 2026.2.1 + new driver" claim now rests on the strongest ear evidence any single session has produced.

For the record when notes/40 gets its final form or the commit message is written: **Nexus ears PASS, 21/21 by filename, clear voices, no additional utterances — covering p0/p1/p2/p4b/p5 artifact classes.** The Validator's waiver stands as the formal gate; your pass gets logged as supplementary confirmation rather than a replacement, so the precedent stays clean — byte-equality remains sufficient for future cache probes, and ears remain what they've always been, the binding word on quality.

With that, C1 has no open questions at all. The board is just your commit call for v1.2.0, C2 whenever appetite says so, and S0 sitting on Grok's runway.