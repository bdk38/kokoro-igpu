# Informative lock snapshot from bdk-server sandbox (not exhaustive of every transitive pin).
# Prefer `pip install -r requirements.txt` on a fresh venv.
# Captured: 2026-08-07 after dual-track rollback (ship = 2026.2.1)
fastapi==0.141.1
numpy==2.4.6
onnx==1.22.0
onnxruntime-openvino==1.24.1
openvino==2026.2.1
openvino-telemetry==2025.2.0
phonemizer==3.4.0
uvicorn==0.52.1
